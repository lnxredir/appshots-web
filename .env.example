# HTTP port exposed on the host (default: 8084)
APPSHOTS_PORT=8084

# Bind address inside the container (default: 0.0.0.0)
APPSHOTS_HOST=0.0.0.0

# Docker image tag (default: latest)
APPSHOTS_TAG=latest
